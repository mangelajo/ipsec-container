set -x
side=$1

ip=$(cat ./vars | grep "^${side}_local_ip" | sed -e 's/.*=//')

sudo docker run --rm --name $side -ti \
    --privileged \
    --cap-add NET_ADMIN \
    --ip=$ip \
    --network=vpn-network \
    -v $(pwd)/$side:/configuration \
    tun bash /${side}.sh
